Given an array of stock prices, this program will select the days in the cycle during which it would be optimal to buy and then sell the stocks.

Usage: stock_picker(stock_prices)
Arguments: an array of ints
Returns: [purchase_day, sell_day]
